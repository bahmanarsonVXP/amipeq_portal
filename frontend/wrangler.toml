name = "amipeq-portal"
compatibility_date = "2024-01-01"
pages_build_output_dir = "out"

# Build-time NEXT_PUBLIC_* : voir `.env.production` (lu par `next build`).
# Les [vars] wrangler servent au runtime Workers/Pages Functions, pas à l’export statique Next.
